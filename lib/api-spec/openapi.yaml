openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: catalog
    description: Customer catalog operations
  - name: orders
    description: Customer order operations
  - name: admin
    description: Store operations and dashboard
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /categories:
    get:
      operationId: listCategories
      tags: [catalog]
      summary: List active product categories
      responses:
        "200":
          description: Categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Category"
  /products:
    get:
      operationId: listProducts
      tags: [catalog]
      summary: List products
      parameters:
        - name: search
          in: query
          required: false
          schema:
            type: string
        - name: category
          in: query
          required: false
          schema:
            type: string
        - name: sort
          in: query
          required: false
          schema:
            type: string
            enum: [popular, price_asc, price_desc, newest, discount]
      responses:
        "200":
          description: Products
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Product"
  /products/{id}:
    get:
      operationId: getProduct
      tags: [catalog]
      summary: Get a product
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Product
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"
        "404":
          description: Product not found
  /orders:
    get:
      operationId: listOrders
      tags: [orders]
      summary: List the current customer's orders
      responses:
        "200":
          description: Orders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Order"
    post:
      operationId: createOrder
      tags: [orders]
      summary: Create a grocery order
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OrderInput"
      responses:
        "201":
          description: Created order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"
        "400":
          description: Invalid order
  /orders/delivery-fee:
    get:
      operationId: getDeliveryFeeEstimate
      tags: [orders]
      summary: Estimate delivery fee for a customer phone number
      parameters:
        - name: phone
          in: query
          required: true
          schema:
            type: string
            minLength: 7
      responses:
        "200":
          description: Delivery fee estimate
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeliveryFeeEstimate"
  /orders/{id}:
    get:
      operationId: getOrder
      tags: [orders]
      summary: Get an order
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"
        "404":
          description: Order not found
  /admin/auth:
    post:
      operationId: adminLogin
      tags: [admin]
      summary: Sign in to the operator dashboard
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminLoginInput"
      responses:
        "200":
          description: Authenticated operator session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminSession"
        "401":
          description: Invalid operator code
  /admin/session:
    get:
      operationId: getAdminSession
      tags: [admin]
      summary: Check the operator session
      responses:
        "200":
          description: Operator session state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminSession"
  /admin/logout:
    post:
      operationId: adminLogout
      tags: [admin]
      summary: Sign out of the operator dashboard
      responses:
        "200":
          description: Signed out
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminSession"
  /admin/dashboard:
    get:
      operationId: getAdminDashboard
      tags: [admin]
      summary: Get store dashboard metrics
      responses:
        "200":
          description: Dashboard metrics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminDashboard"
  /admin/orders:
    get:
      operationId: listAdminOrders
      tags: [admin]
      summary: List store orders
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [new, preparing, courier, delivered, cancelled]
      responses:
        "200":
          description: Orders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Order"
  /admin/orders/{id}/status:
    patch:
      operationId: updateAdminOrderStatus
      tags: [admin]
      summary: Update an order status
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OrderStatusInput"
      responses:
        "200":
          description: Updated order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"
  /admin/categories:
    post:
      operationId: createAdminCategory
      tags: [admin]
      summary: Create a product category
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminCategoryInput"
      responses:
        "201":
          description: Created category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Category"
  /admin/products:
    post:
      operationId: createAdminProduct
      tags: [admin]
      summary: Create a product
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminProductInput"
      responses:
        "201":
          description: Created product
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"
  /admin/products/{id}:
    patch:
      operationId: updateAdminProduct
      tags: [admin]
      summary: Update a product
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminProductUpdateInput"
      responses:
        "200":
          description: Updated product
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"
  /leaderboard/weekly:
    get:
      operationId: getWeeklyLeaderboard
      tags: [orders]
      summary: Get the weekly customer leaderboard
      responses:
        "200":
          description: Weekly customer leaderboard
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WeeklyLeaderboardEntry"
components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    Category:
      type: object
      required: [id, name, slug, icon, product_count]
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        icon:
          type: string
        product_count:
          type: integer
    Product:
      type: object
      required: [id, category_id, category, name, description, image_url, price, old_price, unit, stock, is_popular, is_new]
      properties:
        id:
          type: integer
        category_id:
          type: integer
        category:
          type: string
        name:
          type: string
        description:
          type: string
        image_url:
          type: string
          format: uri
        price:
          type: number
        old_price:
          type: ["number", "null"]
        unit:
          type: string
          enum: [dona, kg, litr, qadoq]
        stock:
          type: number
          minimum: 0
        is_popular:
          type: boolean
        is_new:
          type: boolean
    OrderItem:
      type: object
      required: [product_id, name, image_url, price, quantity, unit, total]
      properties:
        product_id:
          type: integer
        name:
          type: string
        image_url:
          type: string
          format: uri
        price:
          type: number
        quantity:
          type: number
          minimum: 0.001
          multipleOf: 0.000001
        unit:
          type: string
        total:
          type: number
        purchase_mode:
          type: ["string", "null"]
          enum: [quantity, amount, null]
        requested_amount:
          type: ["number", "null"]
          minimum: 0.01
    Order:
      type: object
      required: [id, order_number, customer_name, phone, address, items, status, payment_method, subtotal, delivery_fee, total, created_at]
      properties:
        id:
          type: integer
        order_number:
          type: string
        customer_name:
          type: string
        phone:
          type: string
        address:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/OrderItem"
        status:
          type: string
          enum: [new, preparing, courier, delivered, cancelled]
        payment_method:
          type: string
          enum: [cash, click, payme, uzcard, humo]
        subtotal:
          type: number
        delivery_fee:
          type: number
        total:
          type: number
        created_at:
          type: string
          format: date-time
    OrderInput:
      type: object
      required: [customer_name, items, address, phone, payment_method]
      properties:
        customer_name:
          type: string
          minLength: 2
          maxLength: 80
        items:
          type: array
          minItems: 1
          items:
            type: object
            required: [product_id]
            properties:
              product_id:
                type: integer
              quantity:
                type: number
                minimum: 0.001
                multipleOf: 0.001
              purchase_mode:
                type: string
                enum: [quantity, amount]
              amount:
                type: number
                exclusiveMinimum: 0
                multipleOf: 0.01
        address:
          type: string
          minLength: 3
        phone:
          type: string
          minLength: 7
        payment_method:
          type: string
          enum: [cash, click, payme, uzcard, humo]
    OrderStatusInput:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [new, preparing, courier, delivered, cancelled]
    AdminLoginInput:
      type: object
      required: [code]
      properties:
        code:
          type: string
          minLength: 1
    AdminSession:
      type: object
      required: [authenticated]
      properties:
        authenticated:
          type: boolean
    AdminCategoryInput:
      type: object
      required: [name, slug, icon]
      properties:
        name:
          type: string
          minLength: 2
        slug:
          type: string
          minLength: 2
          pattern: "^[a-z0-9-]+$"
        icon:
          type: string
          minLength: 1
        sort_order:
          type: integer
          minimum: 0
    AdminProductInput:
      type: object
      required: [category_id, name, description, image_url, price, unit, stock]
      properties:
        category_id:
          type: integer
          minimum: 1
        name:
          type: string
          minLength: 2
        description:
          type: string
          minLength: 2
        image_url:
          type: string
          format: uri
        price:
          type: number
          exclusiveMinimum: 0
        old_price:
          type: ["number", "null"]
          minimum: 0
        unit:
          type: string
          enum: [dona, kg, litr, qadoq]
        stock:
          type: number
          minimum: 0
        is_popular:
          type: boolean
        is_new:
          type: boolean
    AdminProductUpdateInput:
      type: object
      minProperties: 1
      properties:
        category_id:
          type: integer
          minimum: 1
        name:
          type: string
          minLength: 2
        description:
          type: string
          minLength: 2
        image_url:
          type: string
          format: uri
        price:
          type: number
          exclusiveMinimum: 0
        old_price:
          type: ["number", "null"]
          minimum: 0
        unit:
          type: string
          enum: [dona, kg, litr, qadoq]
        stock:
          type: number
          minimum: 0
        is_popular:
          type: boolean
        is_new:
          type: boolean
    DeliveryFeeEstimate:
      type: object
      required: [delivery_fee, previous_order_count, is_first_order]
      properties:
        delivery_fee:
          type: number
        previous_order_count:
          type: integer
        is_first_order:
          type: boolean
    WeeklyLeaderboardEntry:
      type: object
      required: [rank, customer_name, phone_masked, order_count, is_winner, prize]
      properties:
        rank:
          type: integer
        customer_name:
          type: string
        phone_masked:
          type: string
        order_count:
          type: integer
        is_winner:
          type: boolean
        prize:
          type: ["string", "null"]
    AdminDashboard:
      type: object
      required: [today_orders, new_orders, preparing_orders, courier_orders, delivered_orders, cancelled_orders, today_revenue, weekly_revenue, monthly_revenue, customer_count, product_count, low_stock_count]
      properties:
        today_orders:
          type: integer
        new_orders:
          type: integer
        preparing_orders:
          type: integer
        courier_orders:
          type: integer
        delivered_orders:
          type: integer
        cancelled_orders:
          type: integer
        today_revenue:
          type: number
        weekly_revenue:
          type: number
        monthly_revenue:
          type: number
        customer_count:
          type: integer
        product_count:
          type: integer
        low_stock_count:
          type: integer

